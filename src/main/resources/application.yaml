# Configuração base. Perfis: local (dev) | prod (produção).
# Sem perfil ativo, usa "local". Rode com: -Dspring.profiles.active=prod para produção.
spring:
  application:
    name: dtec-assistant
  profiles:
    default: local
